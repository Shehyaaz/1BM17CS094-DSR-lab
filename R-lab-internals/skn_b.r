"
DSR Lab Internals
Q. Create a list of numbers, strings and real numbers and name them.
"
n1 <- c(1,2,3,4,5) # vector of numbers
s1 <- c('A','B','C','D','E') # vector of strings
r1 <- c(1.1,1.2,1.3,1.4,1.5)
list1 <- list(num = n1, string = s1, realnum = r1) # create a list
list1 # print the list